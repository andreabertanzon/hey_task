import 'package:flutter/material.dart';

class CategoryCardDialogCards extends StatelessWidget {
  const CategoryCardDialogCards({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}